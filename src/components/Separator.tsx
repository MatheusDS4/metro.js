// third-party
import React from "react";

/**
 * A context-dependent separator. Used in for example
 * popover menus.
 */
export function Separator(): React.ReactNode {
  return (
    <div className="Separator"></div>
  );
}